{
  // "$schema": "http://json.schemastore.org/eslintrc",
  "plugins": ["mocha"],
  "rules": {
    "mocha/no-exclusive-tests": 2,
    "mocha/no-skipped-tests": 1 // for better or worse, some repos have lots of skipped tests
  }
}
